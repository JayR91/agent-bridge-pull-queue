{
  "name": "agent-bridge-pull-queue",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Public HTTPS one-shot pull queue for Agent Bridge mobile-data command pull.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "test": "node --test --test-concurrency=1 test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@vercel/functions": "^3.3.4"
  }
}
